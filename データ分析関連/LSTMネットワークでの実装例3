評価データの予測
評価データの精度評価を行います。評価データについては、モデルの学習と合わせて予測を行っていません。
このため、あらためて学習済モデルのmodelと、評価データの予測結果を評価するevaluate関数を使って、次のように記述して計算します。

評価データの損失, 評価データの正解率 = model.evaluate(評価データの説明変数, 評価データの目的変数)

# 評価データの正解率の計算
loss, accuracy = model.evaluate(X_test_t, y_test_t)

# 評価データの正解率の表示
print('評価データの正解率:', accuracy)
