学習・評価データの分割
時系列予測についてはデータの分割も重要となります。
ランダムでデータを分割すると、時系列成分が崩れてしまうので、基本的に時間を軸としてデータを分割しましょう。

今回は2015年から2017年までのデータを学習用、2018年までのデータを評価データとしましょう。

# 学習用データ
train = df[df['Date']<'2018-01-01']

# 評価用データ
test = df[df['Date']>='2018-01-01']


