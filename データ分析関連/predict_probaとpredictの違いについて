2値分類の予測モデルを作成しテストデータで予測する際
購入するかしないかを予測する場合
predictでて予測するデータを読み込むと0,1で予測結果が出る
一方predict_probaを使って予測すると各行毎に購入する確率が出力される
その為、購入するかどうかを出力したい場合は、結果をcsvファイルに出力する際には以下の様に書く
pred = ext_clf.predict_proba(X_test)
上記の様に予測した場合、
submit[1] = pred[:,1]
この様な形でファイルに出力するとpredictと同じ様に0,1で出力される
